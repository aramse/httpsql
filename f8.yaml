general:
  name: httpsql
volumes:
  - name: postgres-conf
    path: config/postgres.conf
components:
  - type: template
    template: postgres
    name: db
    vars:
      CONFIG: postgres-conf
  - type: stateless
    name: api
    containers:
      - dir: ./
        readyCheck:
          httpGet:
            port: 80
            path: /
    expose:
      - servicePort: 80
        containerPort: 80
        external: true
      - servicePort: 8000
        containerPort: 8000
        metricsPath: /metrics
    instances: 3
tests:
  - name: func
    containers:
      - dir: ./tests/func
  - name: perf
    containers:
      - dir: ./tests/perf
    instances: 2
